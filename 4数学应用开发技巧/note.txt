     在gcc下用到数学函数，如sqrt。在gcc时要加上 -lm 参数，这样告诉编译器我要用到数学函数了。否则会虽然include了math.h,也会无法编译过。

06.c无法使用，首先是没有itoa函数，无法编译成功，其次用snprintf代替后，编译完成后没有结果。
08.c的例子没有写完，就不写了
12.c,编译成功，但是无法运行
从15.c里面可以得知，在c语言里面，1e-1是浮点数的表示方法。
17.c是求解二元一次方程，但是里面没有写，准备以后自己写。
25.c运行无法成功，主要是执行break语句直接跳出了循环导致的
