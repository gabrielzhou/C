06.c里面的函数kbhit是windows里面的，所以导致无法运行
07.c是创建双向链表，其中要 学习的点，其一是typedef可以已有类型定义行的类型，这个程序就是通过结构体struct node来定义一个新类型stud
11.c无法运行，会导致段错误，需要修改。
i++和++i的区别，i++是返回原来的值再进行自加，而++i则是返回自家后的值；实质是i++首先将i的值赋给一个临时的变量，然后再进行自加，最后再返回临时值，所以总共有三步，而++i则是先进行自加，然后将i这个变量返回，总共只有两步；当然现代的编译器都会进行优化。
12.c没有好好的运行，需要寻找原因，改写从程序
13.c里面有一大堆错误，无法修改，以后重新改写
